# 任务管理应用：提示词设计与开发过程复盘

# 第一次对话

目标：让 AI 分析需求和设计技术架构，生成一份需求和设计文档

## 第 1 轮

### 提示词

调用 `grilling` 技能，提出要开发一个任务管理应用，并给出以下需求：

- 任务增删改查，标题必填、描述选填；
- 待办、进行中、完成三种状态；
- 高、中、低三档优先级；
- 三列看板，拖拽改变状态；
- 深色模式并记住选择；
- 使用浏览器保存数据；
- 技术栈为 Vue 3、Vite、Tailwind CSS 和 `localStorage`；
- 要求**分析需求**和**设计架构**。

### AI 做的事情

- 读取并遵循 `grilling` 技能；
- 没有立即输出架构；
- 提出第 1 组问题（Q1–Q5），确认产品范围、交付标准、设备范围、功能边界和 JSON 备份需求。

## 第 2 轮

### 提示词

回答 Q1–Q5：

- 单用户、单浏览器、无账号、无服务端；
- 课程级 MVP；
- 只针对桌面浏览器；
- 首版严格使用已确定的业务字段；
- 支持导入和导出 JSON。

### AI 做的事情

- 记录第一组产品范围决策；
- 提出第 2 组问题（Q6–Q14）；
- 询问开发语言、状态管理、排序规则、表单方式、字段限制、删除保护、导入方式、主题规则和浏览器范围。

## 第 3 轮

### 提示词

回答了 Q6–Q14：

- 使用 TypeScript；
- 使用 Pinia；
- 支持跨列和列内排序，新任务放在待办列顶部；
- 新增和编辑使用共用弹窗；
- 标题最长 50 字，描述最长 1000 字；
- 删除前显示确认弹窗；
- 导入数据时完全替换；
- 首次主题跟随操作系统；
- 支持最新版 Chrome、Edge、Firefox 和 Safari。

### AI 做的事情

- 记录交互和工程技术决策；
- 提出第 3 组问题（Q15–Q21）；
- 询问主题的完整行为、拖拽实现、任务数组结构、新任务默认值、非拖拽状态修改、保存时机和自动化测试。

## 第 4 轮

### 提示词

回答了 Q15–Q21：

- 首次访问跟随系统主题；
- 使用 Vue Draggable/SortableJS；
- 使用单一 `Task[]`；
- 新任务默认进入待办顶部、默认中优先级；
- 编辑弹窗可以修改状态；
- 每次变更后立即保存；
- 使用精简的自动化测试。

### AI 做的事情

- 记录拖拽、数据结构、持久化和测试决策；
- 提出第 4 组问题（Q22–Q28）；
- 询问导出内容、导入校验、损坏数据处理、保存失败处理、首次空状态、优先级显示和时间字段。

## 第 5 轮

### 提示词

回答了 Q22–Q28：

- 只导出任务；
- 导入文件必须整体校验；
- 损坏数据不能被静默清空；
- 保存失败时保留内存任务并显示警告；
- 首次进入显示真正的空看板；
- 优先级同时显示颜色和文字；
- 不保存任务的创建时间和更新时间。

### AI 做的事情

- 汇总此前所有决定；
- 整理任务模型、交互规则、主题规则、存储策略和测试范围；
- 输出需求基线；
- 请求在生成正式设计文档前确认需求基线。

## 第 6 轮

### 提示词

确认需求基线，并要求：

> 把输出整理成一份完整的设计文档。

### AI 做的事情

- 创建完整设计文档 `task-management-app-design.md`；
- 文档包含功能需求、非功能需求、总体架构、数据模型、Pinia 设计、排序算法、持久化、异常恢复、组件设计、目录结构、测试策略、验收标准和实施顺序；

# 第二次对话

## 第 1 轮

### 提示词

提供设计文档路径，要求按照文档实现任务管理系统，并在存在歧义或问题时询问。

### AI 做的事情

- 读取设计文档；
- 创建 Vue 3、Vite、TypeScript、Tailwind CSS 和 Pinia 项目；
- 实现三列任务看板、任务增删改查、拖拽排序、优先级、主题切换、浏览器本地保存及 JSON 导入导出；
- 添加自动化测试并检查项目构建结果。

## 第 2 轮

### 提示词

要求生成可直接访问的网址，并在发布前将当前项目源代码上传到：

`https://github.com/Hwai123/Task_management_app`

### AI 做的事情

- 整理准备上传的项目源代码；
- 配置网站发布所需文件；
- 开始连接 GitHub 仓库和网站托管服务。

## 第 3 轮

### 提示词

调用 GitHub 连接并要求“继续”。

### AI 做的事情

- 将项目源代码上传到 GitHub 仓库；
- 发布任务管理网站；
- 检查网站能够通过公开网址访问。

## 第 4 轮

### 提示词

指出优先级颜色没有实现，要求每个任务的背景色根据优先级变化。

### AI 做的事情

- 为高、中、低优先级分别设置任务卡片背景色；
- 保留优先级文字标识；
- 重新运行测试和构建；
- 将修改后的版本重新发布到原网站地址。

## 第 5 轮

### 提示词

调用 `grilling` 技能，要求将网站打包成任何人都能使用的应用。

### AI 做的事情

- 读取并使用 `grilling` 技能；
- 针对交付形式、公开范围和数据保存方式提出第 1 组选择题。

## 第 6 轮

### 提示词

回答选择题

### AI 做的事情

- 记录这组选择；
- 继续询问应用平台、安装方式和发布方式等打包细节。

## 第 7 轮

### 提示词

修改并确认选择

### AI 做的事情

- 将交付目标调整为同时提供可安装网页应用和 Windows 桌面应用；
- 确认公开发布，并使用本机数据存储；
- 继续询问离线能力、安装包格式、版本发布和数据迁移方式。

## 第 8 轮

### 提示词

要求后续选项全部采用 `grilling` 推荐方案

### AI 做的事情

- 采用 PWA、离线缓存、Tauri 2、Windows x64 和 NSIS 安装包方案；
- 采用 GitHub Releases 分发 Windows 安装包；
- 采用 JSON 导入导出在网页版和桌面版之间迁移数据；
- 继续确认发布名称、签名和更新策略。

## 第 9 轮

### 提示词

再次要求全部采用 `grilling` 推荐方案

### AI 做的事情

- 确认应用名称为“任务流 / Task Flow”；
- 设置发布者为 `Hwai123`，应用标识为 `com.hwai123.taskflow`；
- 确认首个版本为未签名的 `v1.0.0`，采用手动更新方式；
- 整理最终实施方案并等待开始确认。

## 第 10 轮

### 提示词

确认开始实施。

### AI 做的事情

- 添加 Web App Manifest、Service Worker、安装按钮和离线缓存；
- 生成并配置应用图标；
- 添加 Tauri 2 桌面应用配置和 Windows NSIS 打包配置；
- 添加 GitHub Actions Windows 自动构建及 Release 发布流程；
- 更新项目说明和忽略文件。
- 完成 PWA 和 Windows 桌面版相关代码；
- 运行构建和 17 项自动化测试；
- 将更新后的源代码推送到 GitHub；
- 发布新版网站并设置为公开访问；
- 准备将仓库改为公开并发布 `v1.0.0`。

## 第 11 轮

### 提示词

确认将 GitHub 仓库设为公开，并发布 `v1.0.0`。

### AI 做的事情

- 打开 GitHub 仓库设置；
- 执行将仓库从私有改为公开的操作；
- 在 GitHub 要求身份验证时，将验证步骤交给用户完成。

## 第 12 轮

### 提示词

要求“继续”。

### AI 做的事情

- 继续检查 GitHub 仓库公开设置流程；
- 确认 GitHub 仓库已经公开；
- 启动 GitHub Actions Windows 打包流程；
- 持续检查构建状态，确认依赖安装、测试和 Windows 安装包生成成功；
- 发布 `Task Flow v1.0.0`；
- 核对 Windows x64 安装包下载地址、版本页面、源代码仓库和在线网站地址；
- 返回可直接使用的网站与安装包链接，并说明未签名安装包可能触发 Windows SmartScreen 提示。


